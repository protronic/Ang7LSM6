import { JsonCompactPipe } from './json-compact.pipe';

describe('JsonCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
